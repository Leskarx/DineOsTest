const axios = require('axios');

async function test() {
  try {
    // Assuming there's a login to get token, but I don't have credentials.
    // I will mock the typeorm query directly to see what TypeORM returns.
  } catch (e) {
    console.error(e);
  }
}
test();
